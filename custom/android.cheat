% custom, android

; ============================================================================

# 重启 adb 服务并连接到 adb shell
adb kill-server && adb server && adb shell

# 转发 Inspeckage 的服务端口到宿主机
adb forward tcp:<port> tcp:<port>

; ============================================================================
